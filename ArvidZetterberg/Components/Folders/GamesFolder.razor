<Folder>
	<IconLink ContentType="typeof(SpinningCubes)" Name="Cubes" Icon="fa-heart"
		WindowStartMeasures="@(new() { Height = 300, Width = 400 })" />
</Folder>
@code {
	[Parameter]
	[EditorRequired]
	public EventCallback<WindowContent> OnWindowOpen { get; set; }
}